apiVersion: v1
kind: Service
metadata:
  name: inference
  namespace: quant-studio
  labels: { app: inference }
spec:
  type: LoadBalancer        # provisions an AWS ELB -> public URL for the browser
  selector: { app: inference }
  ports:
    - name: http
      port: 80
      targetPort: 8000

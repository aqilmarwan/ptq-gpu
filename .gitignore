# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Build/benchmark pipeline run artifacts (Metaflow / Ray / MLflow)
.metaflow/
mlruns/
mlflow.db
pipelines/out/

# LoRA training instance images (bring your own)
pipelines/data/

# Built TensorRT engine bundles (staged locally before S3 publish) + engine cache
pipelines/engines_out/
inference/engines/

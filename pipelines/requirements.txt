# Offline build/benchmark pipeline. Install on the build box, NOT in the
# inference image. The real plane also needs inference/requirements-gpu.txt
# (torch + diffusers + optimum-quanto) for actual builds; --demo needs neither.
metaflow>=2.12
ray>=2.9
mlflow>=2.14
ruamel.yaml>=0.18
pyyaml>=6.0

# LoRA training (real plane, train_lora.py). Pair torchvision with your torch build.
peft>=0.11
torchvision>=0.19

# Engine build (real plane, build_engines.py): SDXL -> ONNX -> TensorRT -> S3.
# Also needs the serving stack from inference/requirements-gpu.txt (torch, tensorrt)
# plus diffusers/transformers for the one-time ONNX export.
diffusers>=0.31
transformers>=4.44
accelerate>=0.33
onnx>=1.16
onnxscript>=0.1   # torch.onnx.export (dynamo path) needs this
boto3>=1.34
